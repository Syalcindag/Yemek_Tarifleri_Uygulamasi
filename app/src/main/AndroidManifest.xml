<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sefapp.animationbutton">

    <application
        android:allowBackup="true"
        android:icon="@drawable/usta2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity" android:theme="@style/MyTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menu"
                android:theme="@style/MyTheme"
          />
        <activity android:name=".Sicakcorba"
            android:theme="@style/MyTheme"/>
        <activity android:name=".MainActivity"
            android:theme="@style/MyTheme"/>
        <activity android:name=".Ezogelin"
            android:theme="@style/Base.TextAppearance.AppCompat.Widget.PopupMenu.Large"/>
        <activity android:name=".YemekTarifleri"
            android:theme="@style/MyTheme"/>
        <activity android:name=".TatliTarifleri"
            android:theme="@style/MyTheme"/>
    </application>

</manifest>